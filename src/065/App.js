import React from 'react';
import Promise from './R065_Promise';

export default function () {
    return (
        <div>
            <h1>Start React 200!</h1>
            <p>065 - Promise catch 사용하기</p>
            <Promise />
        </div>
    );
}