import React from 'react';
import Promise from './R064_Promise';

export default function () {
    return (
        <div>
            <h1>Start React 200!</h1>
            <p>064 - Promise then 사용하기</p>
            <Promise />
        </div>
    );
}